# Drop-in from ~/.dotfiles: the unit itself lives in the project's repo, this
# only routes its failures to the notebook (see bin/unit-oncall).
[Unit]
OnFailure=unit-oncall@%n.service
OnSuccess=unit-oncall-clear@%n.service
