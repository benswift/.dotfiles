# -*- mode: snippet; require-final-newline: nil -*-
# name: temporal-recursion-now-xtlang
# key: trnx
# binding: direct-keybinding
# --
(bind-func ${1:fun_name}
  (lambda (delta_t$2)
    $0
    (callback (+ (now) delta_t) $1 delta_t$2)))

($1 ${3:44100})