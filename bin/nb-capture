#!/usr/bin/env -S uv run
import subprocess
import sys

note = sys.argv[1]
url = sys.argv[2] if len(sys.argv) > 2 else None

content = f"{note}\n{url}" if url else note
subprocess.run(["nb", "daily", content])
